<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="firstportfolio.wordcharger.repository.CommentsMapper">

    <select id="findCommentsByPostId" resultType="CommentDTO">
        SELECT * FROM comments
        WHERE post_id = #{postId}
        ORDER BY create_date ASC
    </select>

    <insert id="insertComment">
        INSERT INTO comments
        (id, post_id, member_id, content, create_date, parent_comment_id)
        VALUES (COMMENT_SEQUENCE.NEXTVAL, #{postId}, #{memberId}, #{content}, SYSDATE, NULL)
    </insert>

    <insert id="insertComment2">
        INSERT INTO comments
            (id, post_id, member_id, content, create_date, parent_comment_id)
        VALUES (COMMENT_SEQUENCE.NEXTVAL, #{postId}, #{memberId}, #{content}, SYSDATE, #{parentCommentId})
    </insert>

    <select id="findPostIdByMemberId" resultType="Integer">
        SELECT post_id FROM comments
        WHERE member_id = #{memberId}
    </select>



</mapper>